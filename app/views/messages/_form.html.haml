- form_for(:message, url: :conversations) do |f|
  = f.textfield :subject, placeholder: ""
  = f.textfield :body, placeholder: ""
  .form-actions
    = f.button :submit, class: 'btn-primary'
    = submit_tag 'Cancel', type: :reset, class: 'btn btn-danger'